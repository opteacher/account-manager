# 环境变量说明
# 复制此文件为 .env 并根据实际情况修改配置

# ===== 数据库配置 =====
# 数据库类型 (sqlite 或 mysql)
# sqlite: 使用本地 SQLite 文件数据库，无需外部数据库服务器
# mysql: 使用 MySQL 数据库，需要配置 DB_HOST, DB_PORT, DB_USERNAME, DB_PASSWORD
DB_TYPE=sqlite

# 数据库名称（sqlite 为数据库文件名，mysql 为数据库名）
DB_DATABASE=account-manager.db

# 数据库用户名
DB_USERNAME=root

# 数据库密码（对应 docker-compose.dev.yml 中的 MYSQL_ROOT_PASSWORD）
DB_PASSWORD=account123

# 数据库主机（开发环境使用 localhost）
DB_HOST=localhost

# 数据库端口
DB_PORT=3306

# ===== 应用配置 =====
# Vite 项目标识
VITE_PJT=login_platform

# 服务器密钥（用于密码加密）
SERVER_SECRET=account-manager-secret-key-change-in-production

# 管理员用户名
ADMIN=admin

# ===== 后端 URL 配置（仅用于旧版 HTTP API，当前已迁移到 IPC） =====
# 如果需要连接外部后端，取消注释以下行
# VITE_HLW_URL=http://localhost:8051
# VITE_GAW_URL=http://localhost:8051
# VITE_GZW_URL=http://localhost:8051

# ===== 开发模式配置 =====
# Vite 开发服务器地址
# VITE_DEV_SERVER_URL=http://localhost:5173

